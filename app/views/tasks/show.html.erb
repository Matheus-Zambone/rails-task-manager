<div class="container">
<h1><%= @task.title %></h1>

<p><%= @task.details %></p>

<%= link_to 'Edit this task', edit_task_path(@task) %> |

<%= link_to 'Delete', task_path(@task), data: {turbo_method: :delete, turbo_confirm: "Tem certeza?"} %> |

<%= link_to 'Back to list', tasks_path %>
</div>
